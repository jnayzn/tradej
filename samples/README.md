# Sample data

`mt5_history.csv` is a small fictitious MT5 export you can drag into the
**Import** modal in the dashboard to populate the journal with realistic data
(20 trades across 4 symbols, with one revenge-trade pattern that triggers the
analytics warning).
