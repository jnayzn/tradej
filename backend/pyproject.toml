[tool.ruff]
target-version = "py312"
line-length = 100
exclude = [
  ".venv",
  "venv",
  "**/migrations",
  "staticfiles",
]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "W"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"trades/tests.py" = ["E501"]
